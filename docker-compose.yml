services:
  backend:
    build: ./backend
    container_name: ai-api-backend
    ports:
      - "8000:8000"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}

  frontend:
    build: ./frontend
    container_name: ai-api-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
